# We are setting up wordpress, MYSQL and PHPMyadmin using a docker compose yaml file.

# The following commands are performed:

mkdir foldername
touch docker-compose.yaml

docker images
docker-compose up -d
docker ps

localhost:8000
localhost:8080

